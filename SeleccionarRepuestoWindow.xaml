<Window x:Class="RepuestosMSP.Views.SeleccionarRepuestoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Seleccionar Repuesto" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Lista de Repuestos -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Repuesto:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <ComboBox x:Name="cmbRepuestos" Width="250"/>
        </StackPanel>

        <!-- Cantidad -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 0 0 10">
            <TextBlock Text="Cantidad:" VerticalAlignment="Center" Margin="0 0 10 0"/>
            <TextBox x:Name="txtCantidad" Width="100"/>
        </StackPanel>

        <!-- Botón -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Aceptar" Width="80" Click="Aceptar_Click"/>
        </StackPanel>
    </Grid>
</Window>

